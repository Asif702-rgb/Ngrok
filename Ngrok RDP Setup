   Setup Ngrok RDP tunnel
   • Exposes RDP via ngrok
   • Ensure NGROK_AUTH_TOKEN secret is set


      name: Ngrok RDP Setup
   on: [workflow_dispatch]
   jobs:
     deploy:
       runs-on: ubuntu-latest
       steps:
         - name: Checkout code
           uses: actions/checkout@v4
         - name: Setup Ngrok
           run: |
             sudo apt-get update
             sudo apt-get install -y ngrok
             ngrok authtoken ${{ secrets.NGROK_AUTH_TOKEN }}
             ngrok tcp 3389 &
   
